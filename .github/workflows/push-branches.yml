on:
  push:
    branches:
      - "*"

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: '13'
      - name: Package application
        run: |
          mvn -B clean package
